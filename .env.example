# Environment variables for Render deployment
# These will be set in Render dashboard

# Disable Ollama in production (Render doesn't support large models)
OLLAMA_ENABLED=false

# Python version
PYTHON_VERSION=3.12.0
